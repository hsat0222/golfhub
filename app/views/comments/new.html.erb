<h3 class="heading">新規コメント</h3>

<%= form_for(@comment) do |f| %>
  <div class="field">
    <%= f.label :"コメント" %><br />
    <%= f.text_area :comment, size: "80x10"  %>
  </div>

   <div class="actions">
    <%= f.submit "投稿" %>
  </div>

<% end %>